const cssSave = 'css-save';
const settingsSave = 'settings-save';

const cssGet = 'css-get';
const settingsGet = 'settings-get';

const cssBrowse = 'css-browse';
const fcBrowse = 'fc-browse';

const inject = 'inject';
const uninject = 'uninject';

const injectedGet = 'injected-get';
const changingState = 'changing-state';

const responseSuffix = '_response';

export default {
  cssSave,
  settingsSave,
  cssGet,
  settingsGet,
  cssBrowse,
  fcBrowse,
  inject,
  uninject,
  injectedGet,
  changingState,
  responseSuffix,
};
